# Roadmap Review — Post-Implementation Debrief: 006 Conformance corpus + cross-language hardening

**Date**: 2026-06-28 · **Kind**: Post-Implementation Debrief · **Scope**: read-only (proposes; applies nothing)
**Matched ledger entry**: §278 "006 — Conformance corpus + cross-language hardening" `[status: planned]`
**Implementation**: merged as PR #185 (squash `b2092c0`); 21 issues (#164–#184) auto-closed.

## Surfaced Context

| Roadmap §278 says | What was built |
|---|---|
| **Outcome:** a CI gate proving the same logical input through each binding yields identical render + identical `template_hash`/`render_hash`, and that schema-valid/invalid docs are accepted/rejected identically across languages | `ci:conformance` gate (Rust+Python+TS legs) wired into `ci.yml`; all three assert against shared goldens (transitive parity); schema manifest verdicts hold across all three loaders. **Verified green on a real CI runner** (PR #185 checks). |
| **Scope (in):** FFI-marshaling fixtures (datetime/Date/chrono, Decimal, nested, null/undefined/None, int-vs-float); schema round-trip fixtures; CI-gate wiring across the three packages | 5 marshaling fixtures (all named cases), schema manifest reusing the 001 fixtures + YAML twins, `scripts/ci/conformance.sh` + `ci:conformance` moon task + ci.yml job. |
| **Scope (out):** comprehensive render-parity fixtures | None added; spec-002 render set byte-unchanged (SC-006 verified). |
| **Depends on:** 004, 005 | Both implemented; the runners drive their real render/loader paths. |
| **Governed by:** C-01, C-07 | Both upheld (render parity NOT re-tested = C-01; schema round-trip = C-07). |

## Findings

| # | Class | Severity | Detail |
|---|---|---|---|
| F1 | (none) outcome **met** | ✅ | The stated outcome is fully delivered and CI-proven. Marshaling parity + schema round-trip across all three bindings; the seeded-divergence test confirms the gate is non-vacuous (SC-004). |
| F2 | (none) scope **clean** | ✅ | Built exactly scope-in; nothing from scope-out; no scope-creep. |
| F3 | constraint check | ✅ | C-01 upheld (render parity structural, not re-tested); C-07 upheld (schema round-trip is the second guarantee). `ci:check-ffi` green → C-02 incidentally preserved. No `governed-by` violation. |
| F4 | roadmap-stale (MINOR, 💡) | 💡 | The §278 entry is accurate and needs **no scope amendment** — a pleasant contrast to 002/003, whose debriefs found roadmap-stale scope. Two *implementation-level* nuances were discovered and captured to `docs/memory/` (D1 canonical-serialized-form; A1 loader-vs-schema layers) rather than the roadmap; the int-vs-float case was refined (`1 vs 1.0` → `1` vs `2.5`) during the critique. None of these change the entry's outcome/scope wording. The Notes line could optionally cite the two memories, but it is not stale. |

**No 🎯 Must-Address findings. No outcome-miss. No scope-creep. No constraint-violation.**

## Findings Summary

Outcome: **met.** Scope: **clean.** Constraints (C-01, C-07): **upheld.** One 💡 optional note (cite the
D1/A1 memories in the entry's Notes). Capped well under `max_findings=50`.

## Verdict

✅ **PROCEED** — the implementation matches the roadmap entry's outcome and scope with no drift.

## Recommended Actions

1. **Propose status transition: `planned` → `implemented`** (and it qualifies for **`verified`** — outcome
   met AND zero 🎯 Must-Address findings AND full Phase-3 QA passed clean AND CI green on the merge). 002/003/004/005
   used `implemented` after their debrief; recommend the same here: **`[status: implemented]`** with a
   debrief reference, consistent with the established ledger convention. (Apply via `/speckit.roadmap.write`
   — this report does not edit the roadmap.)
2. **Optional (💡 F4):** when bumping, add a Notes line citing memories D1 (canonical-serialized-form
   marshaling) and A1 (loader vs schema-validator layers) and the int-vs-float critique refinement, so the
   ledger points at where those implementation decisions live. No scope/outcome rewording needed.
3. No `iterate`, no `converge`, no `bugfix` — nothing diverged.
