# Task T034–T035 Report — Polish gates + SC reconciliation

**Date**: 2026-06-26 · Run inline by the orchestrator (single gate-command tasks).

## T034 — Full local gate suite

| Gate | Command | Result |
|------|---------|--------|
| Workspace build | `moon run :build` | ✅ GREEN (5 tasks) |
| Kernel tests | `cargo test -p prompting-press-core` | ✅ 41 passed / 0 failed (8 integration suites + lib units + 1 doctest) |
| Codegen freshness | `moon run schemas:codegen-check` | ✅ GREEN (generated shape byte-stable at the kernel path) |
| FFI isolation | `moon run ci:check-ffi` | ✅ GREEN (no pyo3/napi in kernel or consumer) |
| Floating versions | `moon run ci:check-floating-versions` | ✅ GREEN (minijinja/sha2/cargo-deny all pinned) |
| Advisories | `moon run ci:check-advisories` | ✅ GREEN (cargo-deny 0.19.9, no known vulns) |
| clippy | `cargo clippy -p prompting-press-core --all-targets -- -D warnings` | ✅ clean |
| fmt | `cargo fmt --check` | ✅ clean |

## T035 — Success Criteria → backing test reconciliation

| SC | Statement (abbrev) | Backing test(s) | Status |
|----|--------------------|-----------------|--------|
| SC-001 | Determinism: identical inputs → byte-identical output + equal hashes | `hashing.rs` V1.2 (render-twice equality) | ✅ |
| SC-002 | Required-roots excludes loop/set/block locals + globals | `agreement.rs` V2.2 (loop local), V2.3 (set target), V2.4 (globals) | ✅ |
| SC-003 | Undeclared var detectable via required-roots (not silent) | `agreement.rs` V2.6 | ✅ |
| SC-004 | Variant resolution correct across all cases, no silent arm pick | `render.rs` V1.1/V1.3/V1.5 + `render_errors.rs` V1.4 (unknown variant) | ✅ |
| SC-005 | Guard opt-out = body byte-identical; opt-in = separate field, body unchanged | `provenance.rs` V3.2 (opt-out), V3.3 (opt-in body byte-identical) | ✅ |
| SC-006 | Agreement analysis + provenance mutate nothing | `agreement_purity.rs` V2.5 | ✅ |
| SC-007 | Kernel zero pyo3/napi/FFI; no I/O/LLM/etc. | `ci:check-ffi` gate (T002, T034) + no such API in crate | ✅ |
| SC-008 | Every excluded feature rejected with clear error | `excluded_features.rs` (6 constructs × render + analysis) | ✅ |
| SC-009 | Undefined var at render → loud error, never silent empty | `render_errors.rs` V1.7 | ✅ |

**All 9 Success Criteria have ≥1 passing backing test. No gaps.**

## Quickstart scenario coverage

V1.1–V1.8, V2.1–V2.6, V3.1–V3.5, V4.1–V4.3 — all mapped to passing tests across the 8 test files
(`render`, `hashing`, `render_errors`, `agreement`, `agreement_purity`, `provenance`,
`excluded_features`, `scaffold`). No quickstart scenario lacks a test.
