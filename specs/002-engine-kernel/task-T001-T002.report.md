# Task Report: T001 + T002 — Add kernel dependencies & verify build + FFI isolation

**Branch**: 002-engine-kernel
**Date**: 2026-06-26

---

## T001 — Cargo.toml diffs

### `/Cargo.toml` (workspace root)

```diff
 [workspace.dependencies]
 serde = { version = "1", features = ["derive"] }
 serde_json = "1"
+minijinja = { version = "2.21", default-features = false, features = ["builtins", "deserialization", "serde", "std_collections", "adjacent_loop_items"] }
+sha2 = "0.10"
```

### `crates/prompting-press-core/Cargo.toml`

```diff
 [dependencies]
 serde = { workspace = true }
 serde_json = { workspace = true }
+minijinja = { workspace = true }
+sha2 = { workspace = true }
```

---

## T002 — Verification command outputs

### `mise exec -- cargo build -p prompting-press-core`

```
    Updating crates.io index
     Locking 10 packages to latest compatible versions
      Adding block-buffer v0.10.4
      Adding cpufeatures v0.2.17
      Adding crypto-common v0.1.7
      Adding digest v0.10.7
      Adding generic-array v0.14.7 (available: v0.14.9)
      Adding memo-map v0.3.3
      Adding minijinja v2.21.0
      Adding sha2 v0.10.9 (available: v0.11.0)
      Adding typenum v1.20.1
      Adding version_check v0.9.5
   Compiling version_check v0.9.5
   Compiling typenum v1.20.1
   Compiling libc v0.2.186
   Compiling memo-map v0.3.3
   Compiling minijinja v2.21.0
   Compiling generic-array v0.14.7
   Compiling block-buffer v0.10.4
   Compiling crypto-common v0.1.7
   Compiling cpufeatures v0.2.17
   Compiling digest v0.10.7
   Compiling sha2 v0.10.9
   Compiling prompting-press-core v0.0.0 (/Users/sjors/personal/dev/prompting-press/crates/prompting-press-core)
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 12.56s
```

**BUILD: PASS**

---

### `mise exec -- moon run ci:check-ffi`

```
▮▮▮▮ ci:check-ffi (32aa6a4c)
FFI-isolation gate PASSED.
  prompting-press-core: no pyo3, no napi in dependency tree
  prompting-press: no pyo3, no napi in dependency tree
▮▮▮▮ ci:check-ffi (7s 839ms, 32aa6a4c)

Tasks: 1 completed
 Time: 8s 918ms
```

**FFI GATE: PASS**

---

### `mise exec -- cargo tree -p prompting-press-core -i pyo3`

```
error: package ID specification `pyo3` did not match any packages
EXIT:101
```

**pyo3 absent: PASS** (exit 101 = not present in tree, expected pass condition)

---

### `mise exec -- cargo tree -p prompting-press-core -i napi`

```
error: package ID specification `napi` did not match any packages
EXIT:101
```

**napi absent: PASS** (exit 101 = not present in tree, expected pass condition)

---

## Summary

| Check | Result |
|---|---|
| `cargo build -p prompting-press-core` | PASS |
| `moon run ci:check-ffi` | PASS |
| `cargo tree -i pyo3` (kernel) | PASS — absent |
| `cargo tree -i napi` (kernel) | PASS — absent |

All four gates are green. New transitive deps pulled in: minijinja 2.21.0, sha2 0.10.9, and their pure-Rust crypto chain (digest, crypto-common, block-buffer, generic-array, typenum, cpufeatures). No FFI deps introduced.
